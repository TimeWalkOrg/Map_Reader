# python env
env/
__pycache__/
*.pyc

# model checkpoints (auto-download, ~375MB+)
*.pth
checkpoints/

# large rasters — COGs live in the NYC_Maps repo (Git LFS), not here
*.tif
*.tiff
*.tif.aux.xml

# scratch
.DS_Store
*.log

# wrangler scratch
.wrangler/

# overnight batch scratch (big / regenerable)
pilot/results/sam_tiles/
pilot/results/candidates_sam.gpkg
pilot/results/batch_sam.pid

# v4 exploration scratch (regenerable debug crops)
pilot/scratch/

# v5 vision-audit tile renders (regenerable via pilot/audit_vision.py)
pilot/results/audit_v5/tiles/
